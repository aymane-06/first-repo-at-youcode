#include <stdio.h>
#include <math.h>

int main(){
float r,v;
printf("entre le rayon du sphere r: ");
scanf("%f",&r);
  v = (4.0 / 3.0) * M_PI * pow(r, 3);
printf("le volume du sphere est: %f",v);





return 0;
}
